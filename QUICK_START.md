# ⚡ Быстрый старт

## 🚨 Важно: Ошибка "Connection refused"

Если вы видите ошибку `Connection refused` в логах, это означает, что **бэкенд не запущен**.

## ✅ Решение

### Вариант 1: Запустить бэкенд (рекомендуется)

1. Откройте **новый терминал**
2. Перейдите в папку backend:
   ```bash
   cd backend
   ```
3. Установите зависимости (если еще не установлены):
   ```bash
   npm install
   ```
4. Запустите бэкенд:
   ```bash
   npm run dev
   ```
5. Вы должны увидеть:
   ```
   🚀 VPN Backend API running on http://localhost:3000
   ```

6. Теперь перезапустите Flutter приложение

### Вариант 2: Использовать моковые данные (без бэкенда)

Приложение **автоматически** использует моковые данные, если бэкенд недоступен. Просто игнорируйте ошибки в логах - приложение будет работать с тестовыми серверами.

## 🔍 Проверка

### Проверьте, что бэкенд запущен:

```bash
curl http://localhost:3000/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "timestamp": "...",
  "uptime": ...,
  "serversCount": 7
}
```

### Для Android эмулятора:

Бэкенд должен быть доступен на `http://10.0.2.2:3000` (это автоматически преобразуется из `localhost:3000`)

## 📝 Примечания

- Приложение автоматически переключается на моковые данные, если бэкенд недоступен
- Ошибки в логах не критичны, если вы видите список серверов в приложении
- Для production обязательно запустите бэкенд на реальном сервере

## 🎯 Что делать дальше

1. **Если бэкенд запущен** - приложение подключится автоматически
2. **Если бэкенд не запущен** - приложение использует моковые данные (все равно работает!)

