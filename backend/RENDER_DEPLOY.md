# 🚀 Деплой бэкенда на Render

## Быстрый старт

### Вариант 1: Автоматический деплой через render.yaml (Рекомендуется)

1. **Зайдите на [render.com](https://render.com)** и зарегистрируйтесь (можно через GitHub)

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Выберите репозиторий `VPN_front`

3. **Настройки деплоя:**
   - **Name:** `vpn-backend` (или любое другое имя)
   - **Root Directory:** `backend` ⚠️ **ВАЖНО!**
   - **Environment:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Plan:** `Free` (бесплатный тариф)

4. **Переменные окружения:**
   Render автоматически установит:
   - `NODE_ENV=production`
   - `PORT` (автоматически, не нужно указывать)

5. **Нажмите "Create Web Service"**

6. **Дождитесь деплоя** (обычно 2-5 минут)

7. **Получите URL** вашего API (например: `https://vpn-backend.onrender.com`)

---

### Вариант 2: Ручная настройка (если render.yaml не работает)

1. **Создайте новый Web Service на Render**

2. **Настройки:**
   ```
   Name: vpn-backend
   Root Directory: backend
   Environment: Node
   Build Command: npm install
   Start Command: npm start
   Plan: Free
   ```

3. **Переменные окружения:**
   - `NODE_ENV` = `production`
   - `PORT` - НЕ указывайте, Render установит автоматически

4. **Health Check Path:** `/health`

---

## ✅ Проверка работы

После деплоя проверьте:

1. **Health Check:**
   ```
   GET https://your-app.onrender.com/health
   ```
   Должен вернуть: `{"status":"ok",...}`

2. **Получение серверов:**
   ```
   GET https://your-app.onrender.com/api/servers
   ```

---

## 🔧 Настройка CORS (если нужно)

Если ваш фронтенд на другом домене, обновите CORS в `server.js`:

```javascript
app.use(cors({
  origin: 'https://your-frontend-domain.com', // Замените на ваш домен
  methods: ['GET', 'POST', 'PUT', 'DELETE'],
  allowedHeaders: ['Content-Type', 'Authorization'],
}));
```

Или оставьте `origin: '*'` для разработки.

---

## 📝 Важные замечания

1. **Бесплатный тариф Render:**
   - Сервис "засыпает" после 15 минут бездействия
   - Первый запрос после "сна" может занять 30-60 секунд
   - Для продакшена рассмотрите платный тариф

2. **Файловая система:**
   - Папка `data/` создается автоматически при первом запуске
   - Данные сохраняются между перезапусками
   - При пересоздании сервиса данные могут быть потеряны

3. **Логи:**
   - Просматривайте логи в панели Render
   - Все `console.log` будут видны в логах

---

## 🐛 Решение проблем

### Сервис не запускается
- Проверьте логи в панели Render
- Убедитесь, что `Root Directory` = `backend`
- Проверьте, что `package.json` содержит правильный `start` скрипт

### 502 Bad Gateway
- Подождите 1-2 минуты после деплоя
- Проверьте логи на наличие ошибок
- Убедитесь, что сервер слушает на `0.0.0.0` (уже настроено)

### CORS ошибки
- Обновите настройки CORS в `server.js`
- Убедитесь, что фронтенд использует правильный URL API

---

## 🔗 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Node.js на Render](https://render.com/docs/node-version)
- [Environment Variables](https://render.com/docs/environment-variables)

---

## 📞 После деплоя

1. Скопируйте URL вашего API (например: `https://vpn-backend.onrender.com`)
2. Обновите URL API в вашем Flutter приложении
3. Протестируйте все endpoints

**Готово! Ваш бэкенд работает на Render! 🎉**

