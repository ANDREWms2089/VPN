# ✅ Чеклист готовности к деплою на Render

## Файлы для деплоя

- [x] `package.json` - настроен с правильным `start` скриптом
- [x] `server.js` - основной файл сервера
- [x] `render.yaml` - конфигурация для автоматического деплоя (опционально)
- [x] `.env.example` - пример переменных окружения
- [x] `.gitignore` - исключает node_modules и .env

## Проверка кода

- [x] `server.js` использует `process.env.PORT || 3000` ✅
- [x] Сервер слушает на `0.0.0.0` ✅
- [x] Health check endpoint `/health` существует ✅
- [x] CORS настроен ✅
- [x] Все зависимости указаны в `package.json` ✅

## Что нужно сделать перед деплоем

1. **Закоммитьте изменения:**
   ```bash
   git add backend/
   git commit -m "Prepare backend for Render deployment"
   git push
   ```

2. **На Render:**
   - Создайте Web Service
   - Укажите `Root Directory: backend`
   - Выберите план `Free`
   - Дождитесь деплоя

3. **После деплоя:**
   - Проверьте `/health` endpoint
   - Скопируйте URL API
   - Обновите URL в Flutter приложении

## Зависимости

Все необходимые зависимости для `server.js`:
- ✅ express
- ✅ cors
- ✅ dotenv
- ✅ fs (встроенный)
- ✅ path (встроенный)
- ✅ net (встроенный)

**Примечание:** `firebase-admin` и `firebase-functions` не используются в `server.js`, но оставлены в `package.json` для совместимости с другими частями проекта. Они не мешают работе на Render.

## Готово к деплою! 🚀

