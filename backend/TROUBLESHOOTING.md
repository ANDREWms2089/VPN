# 🔧 Устранение проблем с бэкендом

## Проблема: Бэкенд не доступен из Android эмулятора

### Симптомы:
- Ошибка `Connection refused` на `http://10.0.2.2:3000`
- Приложение не может подключиться к бэкенду

### Решения:

#### 1. Убедитесь, что бэкенд запущен

```bash
cd backend
npm install
npm run dev
```

Вы должны увидеть:
```
🚀 VPN Backend API running on http://0.0.0.0:3000
```

#### 2. Проверьте, что бэкенд слушает на всех интерфейсах

Бэкенд должен запускаться на `0.0.0.0`, а не только на `localhost`. Это уже настроено в `server.js`.

#### 3. Проверьте доступность бэкенда

**С хоста (Mac/Windows):**
```bash
curl http://localhost:3000/health
```

**Из Android эмулятора (через adb):**
```bash
adb shell
curl http://10.0.2.2:3000/health
```

#### 4. Проверьте порт

Убедитесь, что порт 3000 свободен:
```bash
# Mac/Linux
lsof -i :3000

# Windows
netstat -ano | findstr :3000
```

Если порт занят, измените PORT в `.env` файле.

#### 5. Проверьте настройки эмулятора

- Убедитесь, что эмулятор имеет доступ к сети
- Попробуйте перезапустить эмулятор
- Проверьте настройки прокси эмулятора

#### 6. Альтернативные способы доступа

**Вариант A: Использовать IP адрес хоста**

1. Узнайте IP адрес вашего компьютера:
   ```bash
   # Mac/Linux
   ifconfig | grep "inet "
   
   # Windows
   ipconfig
   ```

2. Измените `baseUrl` в `api_service.dart`:
   ```dart
   static String get baseUrl {
     if (!kIsWeb && Platform.isAndroid) {
       return 'http://YOUR_IP_ADDRESS:3000/api'; // Замените YOUR_IP_ADDRESS
     }
     return 'http://localhost:3000/api';
   }
   ```

**Вариант B: Использовать порт-форвардинг**

```bash
adb reverse tcp:3000 tcp:3000
```

Затем в `api_service.dart` используйте `localhost:3000` вместо `10.0.2.2:3000`.

#### 7. Проверьте файрвол

Убедитесь, что файрвол не блокирует порт 3000:
- Mac: Системные настройки → Безопасность → Файрвол
- Windows: Панель управления → Брандмауэр Windows

## Быстрая проверка

1. ✅ Бэкенд запущен (`npm run dev`)
2. ✅ Порт 3000 свободен
3. ✅ Бэкенд слушает на `0.0.0.0:3000`
4. ✅ `curl http://localhost:3000/health` работает
5. ✅ Эмулятор имеет доступ к сети

## Если ничего не помогает

Используйте моковые данные - приложение автоматически переключится на них, если бэкенд недоступен. Просто игнорируйте ошибки в логах.

