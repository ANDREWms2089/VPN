# Belchonok VPN - VLESS VPN Application

Современное VPN приложение с поддержкой протокола VLESS, выполненное в оранжевом дизайне с красивыми анимациями и переходами.

## 🌟 Особенности

- ✅ Поддержка протокола VLESS
- ✅ Красивый UI в оранжевых тонах (теплые оранжевые оттенки)
- ✅ Плавные анимации и переходы
- ✅ Отображение скорости подключения
- ✅ Список серверов с индикацией ping
- ✅ Бэкенд API для управления серверами
- ✅ Адаптивный дизайн
- ⚠️ **ВАЖНО**: Сейчас используется симуляция VPN. Для реального подключения требуется интеграция с v2ray/xray core (см. [VPN_INTEGRATION.md](VPN_INTEGRATION.md))

## 📱 Технологии

### Frontend
- **Flutter** - кроссплатформенный фреймворк
- **Provider** - управление состоянием
- **Animations** - плавные анимации
- **HTTP** - работа с API

### Backend
- **Node.js + Express** - REST API
- **CORS** - поддержка кросс-доменных запросов

## 🚀 Установка и запуск

### Важно: Запуск бэкенда и фронтенда

**Бэкенд должен быть запущен перед запуском фронтенда!**

### Шаг 1: Запуск Backend (Node.js)

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. (Опционально) Создайте файл `.env`:
```bash
# Создайте .env файл с содержимым:
PORT=3000
NODE_ENV=development
```

4. Запустите сервер:
```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Или production режим
npm start
```

Сервер будет доступен на `http://localhost:3000`

**Проверьте, что сервер запущен:**
```bash
curl http://localhost:3000/health
```

### Шаг 2: Запуск Frontend (Flutter)

1. Установите зависимости:
```bash
flutter pub get
```

2. Запустите приложение:
```bash
# Для Android эмулятора
flutter run

# Для iOS симулятора
flutter run

# Для веб
flutter run -d chrome
```

**Примечание:** Фронтенд автоматически определит правильный URL бэкенда:
- Android эмулятор: `http://10.0.2.2:3000/api`
- iOS симулятор / Desktop: `http://localhost:3000/api`
- Web: `http://localhost:3000/api`

## 📡 API Endpoints

- `GET /api/servers` - Получить список всех VLESS серверов
- `GET /api/servers/:id/ping` - Проверить ping сервера
- `POST /api/connection` - Управление подключением (connect/disconnect)
- `GET /health` - Проверка здоровья сервера

## 🎨 Дизайн

Приложение использует светло-зеленую цветовую палитру:
- **Primary Green**: `#7ED321`
- **Light Green**: `#B8E986`
- **Pale Green**: `#E8F5E9`
- **Mint Green**: `#C8E6C9`
- **Cucumber Green**: `#A5D6A7`

## 📝 Структура проекта

```
lib/
├── main.dart                 # Точка входа
├── models/                   # Модели данных
│   ├── vless_server.dart
│   └── vpn_status.dart
├── services/                 # Сервисы
│   ├── vpn_service.dart
│   └── api_service.dart
├── providers/               # Провайдеры состояния
│   └── vpn_provider.dart
├── screens/                  # Экраны приложения
│   ├── home_screen.dart
│   └── servers_screen.dart
└── theme/                    # Тема и цвета
    └── app_colors.dart

backend/
├── server.js                 # Express сервер
├── package.json
└── .env.example
```

## 🔧 Настройка

### Подключение к бэкенду

Для подключения к реальному бэкенду, обновите `baseUrl` в `lib/services/api_service.dart`:

```dart
static const String baseUrl = 'https://your-backend-api.com/api';
static const bool useMockData = false; // Использовать реальный API
```

### Реальное VPN подключение

**⚠️ ВАЖНО**: Сейчас приложение использует **симуляцию VPN подключения**. 

Для реального VPN подключения требуется:
1. Интеграция с v2ray/xray core
2. Настройка нативного VPN сервиса (Android/iOS)
3. Обработка разрешений VPN

Подробная инструкция: [VPN_INTEGRATION.md](VPN_INTEGRATION.md)

**Разрешения VPN уже добавлены в манифесты:**
- ✅ Android: `BIND_VPN_SERVICE`, `FOREGROUND_SERVICE`, `WAKE_LOCK`
- ✅ iOS: Network Extensions entitlement

## 📱 Скриншоты

Приложение включает:
- Главный экран с большой кнопкой подключения
- Анимированный индикатор статуса
- Карточки серверов с флагами стран
- Отображение скорости загрузки/выгрузки
- Плавные переходы между экранами

## 🛠️ Разработка

Для добавления новых серверов, отредактируйте массив `servers` в `backend/server.js` или подключите базу данных.

## 📄 Лицензия

Проект создан для демонстрации возможностей Flutter в создании VPN приложений.
