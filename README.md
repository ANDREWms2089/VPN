# Belchonok VPN - VLESS VPN Application

Современное VPN приложение с поддержкой протокола VLESS, выполненное в оранжевом дизайне с красивыми анимациями и переходами.

## 🌟 Особенности

- ✅ **Реальное VPN подключение** через v2ray/xray core
- ✅ Поддержка протокола VLESS (включая Reality, TLS, WS, GRPC)
- ✅ Красивый UI в оранжевых тонах
- ✅ Плавные анимации и переходы
- ✅ Отображение скорости подключения в реальном времени
- ✅ Список серверов с индикацией ping
- ✅ Бэкенд API для управления серверами
- ✅ Адаптивный дизайн
- ✅ Кроссплатформенность (Android и iOS)

## 📱 Технологии

### Frontend
- **Flutter** - кроссплатформенный фреймворк
- **flutter_v2ray** (v1.0.10) - интеграция с v2ray/xray core для реального VPN
- **Provider** - управление состоянием
- **Animations** - плавные анимации
- **HTTP** - работа с API

### Backend
- **PHP** - REST API (планируется)
- **Mock Data** - временно используется для разработки фронтенда

## 🚀 Установка и запуск

### Запуск Frontend (Flutter)

**Примечание:** В настоящее время приложение использует mock данные. PHP бэкенд будет добавлен позже. См. `PHP_BACKEND_REQUIREMENTS.md` для деталей.

1. Установите зависимости:
```bash
flutter pub get
```

2. Запустите приложение:
```bash
# Для Android
flutter run

# Для iOS
flutter run -d ios
```

**⚠️ ВАЖНО:** VPN работает только на **реальных устройствах** (не эмуляторах)!

**Примечание:** Приложение использует mock данные по умолчанию. После создания PHP бэкенда обновите `useMockData = false` в `lib/services/api_service.dart`.

## 📡 API Endpoints (для будущего PHP бэкенда)

См. `PHP_BACKEND_REQUIREMENTS.md` для полного списка требований.

Основные endpoints:
- `GET /api/servers.php` - Получить список всех VLESS серверов
- `GET /api/ping.php?id={id}` - Проверить ping сервера

## 🔐 VPN Функциональность

### Реальное VPN подключение

Приложение использует **реальное VPN подключение** через пакет `flutter_v2ray`, который интегрирует v2ray/xray core:

- ✅ Автоматическая генерация config.json для v2ray/xray
- ✅ Поддержка всех параметров VLESS протокола
- ✅ Реальное туннелирование трафика
- ✅ Мониторинг скорости в реальном времени
- ✅ Автоматическая обработка статусов подключения

### Использование

```dart
// Подключение к VPN
final vpnService = VpnService();
await vpnService.initialize();
await vpnService.connect(server);

// Отключение
await vpnService.disconnect();

// Мониторинг статуса
vpnService.statusStream.listen((status) {
  print('VPN Status: ${status.status}');
  if (status.isConnected) {
    print('Speed: ↑${status.uploadSpeed} KB/s ↓${status.downloadSpeed} KB/s');
  }
});
```

### Разрешения

При первом подключении пользователь должен предоставить VPN разрешение:
- **Android**: Системный диалог запроса разрешения VPN
- **iOS**: Автоматическая настройка через Network Extension

## 🎨 Дизайн

Приложение использует светло-зеленую цветовую палитру:
- **Primary Green**: `#7ED321`
- **Light Green**: `#B8E986`
- **Pale Green**: `#E8F5E9`
- **Mint Green**: `#C8E6C9`
- **Cucumber Green**: `#A5D6A7`

## 📝 Структура проекта

```
lib/
├── main.dart                 # Точка входа
├── models/                   # Модели данных
│   ├── vless_server.dart
│   └── vpn_status.dart
├── services/                 # Сервисы
│   ├── vpn_service.dart      # ✅ Реальный VPN сервис (flutter_v2ray)
│   ├── config_generator.dart # ✅ Генератор config.json для v2ray/xray
│   └── api_service.dart
├── providers/               # Провайдеры состояния
│   └── vpn_provider.dart
├── screens/                  # Экраны приложения
│   ├── home_screen.dart
│   └── servers_screen.dart
└── theme/                    # Тема и цвета
    └── app_colors.dart

# Backend будет реализован на PHP
# См. PHP_BACKEND_REQUIREMENTS.md для деталей
```

## 🔧 Настройка

### Подключение к PHP бэкенду

После создания PHP бэкенда, обновите настройки в `lib/services/api_service.dart`:

```dart
static String get baseUrl {
  return 'https://your-php-backend.com/api';
}
static const bool useMockData = false; // Использовать реальный API
```

См. `PHP_BACKEND_REQUIREMENTS.md` для полных требований к PHP бэкенду.

### Настройка VPN

VPN полностью настроен и работает через пакет `flutter_v2ray`. Все необходимое автоматически настраивается при установке зависимостей.

**Разрешения VPN уже добавлены в манифесты:**
- ✅ Android: `BIND_VPN_SERVICE`, `FOREGROUND_SERVICE`, `WAKE_LOCK`
- ✅ iOS: Network Extensions capability

## 📱 Экраны

Приложение включает:
- Главный экран с большой кнопкой подключения
- Анимированный индикатор статуса
- Карточки серверов с флагами стран
- Отображение скорости загрузки/выгрузки в реальном времени
- Плавные переходы между экранами

## 🛠️ Разработка

Для добавления новых серверов, отредактируйте метод `_getMockServers()` в `lib/services/api_service.dart` или используйте настройки в приложении для добавления VLESS конфигураций.

## 🐛 Отладка

### Android
```bash
adb logcat | grep -i v2ray
```

### iOS
- Используйте Console.app для просмотра логов
- Или в Xcode: Window → Devices and Simulators → View Device Logs

## ⚠️ Важно

1. **Реальное устройство**: VPN работает только на реальных устройствах (не эмуляторах)
2. **Разрешения**: Пользователь должен предоставить VPN разрешение при первом подключении
3. **Тестирование**: Обязательно тестируйте на реальном устройстве

## 📄 Лицензия

Проект создан для демонстрации возможностей Flutter в создании VPN приложений.

---

**Приложение готово к использованию! 🎉**
