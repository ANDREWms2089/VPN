# 🚀 Быстрый старт

## Запуск приложения

### 1️⃣ Запустите Backend

Откройте терминал и выполните:

```bash
cd backend
npm install
npm run dev
```

Вы должны увидеть:
```
🚀 VPN Backend API running on http://localhost:3000
📡 Available endpoints:
   GET  /api/servers
   GET  /api/servers/:id/ping
   POST /api/connection
   GET  /health
```

### 2️⃣ Запустите Frontend

Откройте **новый** терминал и выполните:

```bash
flutter pub get
flutter run
```

### 3️⃣ Проверка работы

1. Backend должен быть доступен на `http://localhost:3000`
2. Проверьте health endpoint: `http://localhost:3000/health`
3. В приложении Flutter вы должны увидеть список серверов из бэкенда

## 🔧 Устранение проблем

### Backend не запускается

- Убедитесь, что Node.js установлен: `node --version`
- Проверьте, что порт 3000 свободен
- Попробуйте изменить PORT в `.env` файле

### Frontend не подключается к Backend

- Убедитесь, что backend запущен и доступен
- Для Android эмулятора используйте `http://10.0.2.2:3000/api`
- Проверьте логи в консоли Flutter

### Серверы не загружаются

- Проверьте консоль backend на наличие ошибок
- Проверьте, что `useMockData = false` в `api_service.dart`
- Проверьте логи в Flutter DevTools

## 📝 Примечания

- Backend должен быть запущен **до** запуска фронтенда
- В production измените `baseUrl` в `api_service.dart` на реальный URL сервера
- Для тестирования можно временно установить `useMockData = true`

