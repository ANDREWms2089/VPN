# 🔤 Настройка шрифта Sansation

## Установка шрифта

Шрифт Sansation нужно добавить в проект вручную:

### 1. Скачайте шрифт

Скачайте файлы шрифта Sansation:
- `Sansation-Regular.ttf`
- `Sansation-Bold.ttf`
- `Sansation-Italic.ttf` (опционально)

Ссылки для скачивания:
- https://www.fontsquirrel.com/fonts/sansation
- https://www.dafont.com/sansation.font

### 2. Создайте папку для шрифтов

```bash
mkdir -p fonts
```

### 3. Поместите файлы шрифтов

Поместите файлы `.ttf` в папку `fonts/`:
```
fonts/
  - Sansation-Regular.ttf
  - Sansation-Bold.ttf
```

### 4. Обновите pubspec.yaml

Добавьте в `pubspec.yaml`:

```yaml
flutter:
  fonts:
    - family: Sansation
      fonts:
        - asset: fonts/Sansation-Regular.ttf
        - asset: fonts/Sansation-Bold.ttf
          weight: 700
```

### 5. Установите зависимости

```bash
flutter pub get
```

## Временное решение

Пока шрифт не добавлен, приложение будет использовать системный шрифт. Все тексты уже настроены на использование `fontFamily: 'Sansation'`, поэтому после добавления шрифта он автоматически применится.

## Альтернатива: Использовать похожий шрифт из Google Fonts

Если не хотите добавлять локальный шрифт, можно использовать похожий шрифт:

```dart
// Вместо GoogleFonts.sansation() использовать:
GoogleFonts.roboto() // или другой похожий шрифт
```

Но рекомендуется использовать оригинальный Sansation для соответствия дизайну.

